[user]
  name = Evan Pavlica
  email = evan@pavlica.us
  signingkey = F461D89F2EFA35F2

[credential]
  # helper = store --file ~/.git-credential-store
  helper = osxkeychain

[color]
  ui = true

[push]
  default = upstream

[merge]
	conflictStyle = zdiff3
  # ff = false

[rebase]
  autosquash = true

[diff]
  renames = true
  confictstyle = patience
  tool = vimdiff

[core]
  editor = vim
  excludesfile = ~/.gitignore_global
  ignorecase = false
  whitespace = trailing-space,space-before-tab,
	pager = delta

[apply]
  whitespace = nowarn

[format]
  pretty = %Cblue%h%Creset %Cgreen[%ad]%Creset (%an) %s

[alias]
  branches = for-each-ref --sort=-committerdate --format=\"%(color:blue)%(authordate:relative)\t%(color:red)%(authorname)\t%(color:white)%(color:bold)%(refname:short)\" refs/remotes
  gr = log --graph --date-order -C -M --pretty=format:\"%Cblue%h%Creset %Cgreen[%ad]%Creset (%an) %Cgreen%d%Creset %s\" --all --date=short
  lol = log -n 10 --graph --pretty=\"%C(yellow)%h %C(reset)| %s | %C(blue)%an %C(reset)| %C(green)%ar\"
  mru = for-each-ref --sort=-committerdate --count=10 refs/heads/ --format='%(HEAD) %(color:yellow)%(refname:short)%(color:reset) - %(color:red)%(objectname:short)%(color:reset) - %(contents:subject) - %(authorname) (%(color:green)%(committerdate:relative)%(color:reset))'

[filter "lfs"]
  clean = git-lfs clean -- %f
  smudge = git-lfs smudge -- %f
  required = true
  process = git-lfs filter-process

[commit]
  gpgsign = true
  template = ~/.gitmessage

[init]
  defaultBranch = main

[url "https://github.com/e-pavlica"]
  insteadOf = https://github.com/xicreative
[credential "https://github.com"]
  helper = 
  helper = !/Users/epavlica/.homebrew/bin/gh auth git-credential
[credential "https://gist.github.com"]
  helper = 
  helper = !/Users/epavlica/.homebrew/bin/gh auth git-credential
[http]
  postBuffer = 157286400
[interactive]
  diffFilter = delta --color-only
[delta]
  navigate = true
# Include the config file generated by the tinty hook, which contains theme-related configuration:
[include]
  path = ~/.local/share/tinted-theming/tinty/tinted-delta-configs-file.gitconfig
