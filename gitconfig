[user]
  # name = Evan Pavlica & Daniel Tea
  # name = Evan Pavlica & Ron Dollete
  # name = Evan Pavlica & William Andrews
  name = Evan Pavlica
  email = evan@xicreative.net

[credential]
  helper = cache

[color]
  ui = true

[push]
  default = upstream

[merge]
  ff = only

[rebase]
  autosquash = true

[diff]
  renames = true
  confictstyle = patience
  tool = vimdiff

[core]
  editor = vim
  excludesfile = /Users/epavlica/.gitignore_global

[apply]
  whitespace = nowarn

[format]
  pretty = %Cblue%h%Creset %Cgreen[%ad]%Creset (%an) %s

[alias]
  gr = log --graph --date-order -C -M --pretty=format:\"%Cblue%h%Creset %Cgreen[%ad]%Creset (%an) %Cgreen%d%Creset %s\" --all --date=short
  lol = log -n 10 --graph --pretty=\"%C(yellow)%h %C(reset)| %s | %C(blue)%an %C(reset)| %C(green)%ar\"

[filter "lfs"]
  clean = git-lfs clean %f
  smudge = git-lfs smudge %f
  required = true
